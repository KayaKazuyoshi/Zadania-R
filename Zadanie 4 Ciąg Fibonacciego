fibo <- function(k) {
  if(k>= 3) {
    fibo(k-1)+fibo(k-2)
  } else {
  return(1)
  }
}


fib2 <- function(k) {
    for(i in k) {
      if(k>=3){
        return(fib2(k-1)+fib2(k-2))
    } else {
        return(1)
      }
    }
  }
